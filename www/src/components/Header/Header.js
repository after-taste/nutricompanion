import React from 'react';
import Flex from '@components/Flex/Flex';

export default () => {
    return (
        <React.Fragment>
            <Flex>
                <h1>TITLE</h1>
            </Flex>
        </React.Fragment>
    );
};